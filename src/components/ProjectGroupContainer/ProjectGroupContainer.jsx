import Card from '../Card/Card';
import './ProjectGroupContainer.scss';

const ProjectGroupContainer = () => {
  return <Card className="ProjectGroupContainer">ProjectGroupContainer</Card>;
};

export default ProjectGroupContainer;
